sources:
  2d_daily_max:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk    
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_2d_1d_max_remap025.parquet
    driver: zarr
  2d_daily_mean:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_2d_1d_mean_remap025.parquet
    driver: zarr
  2d_daily_min:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_2d_1d_min_remap025.parquet
    driver: zarr
  plev19_daily_mean:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_plev19_1d_mean_remap025.parquet
    driver: zarr     
  2d_monthly_mean:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_2d_1mth_mean_remap025.parquet
    driver: zarr  
  pfull_phalf_monthly_mean:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_pfull_phalf_1mth_mean_remap025.parquet
    driver: zarr 
  plev19_monthly_mean:
    args:
      chunks: auto
      consolidated: false
      storage_options:
        lazy: true
        remote_options:
          slk_cache: "/scratch/{{ env(USER)[0] }}/{{ env(USER) }}/INTAKE_CACHE"
        remote_protocol: slk
      urlpath: reference:://work/bm1344/k202193/Kerchunk/control_new_set/archived_ICON_control/atm_plev19_1mth_mean_remap025.parquet
    driver: zarr  